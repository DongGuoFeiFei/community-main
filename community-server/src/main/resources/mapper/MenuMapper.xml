<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.communityserver.mapper.MenuMapper">

    <select id="selectUserMenus" resultType="com.example.communityserver.entity.model.Menu">
        SELECT m.menu_id,
               m.menu_name,
               m.parent_id,
               m.order_num,
               m.path,
               m.component,
               m.menu_type,
               m.visible,
               m.perms,
               m.icon
        FROM menu m
                 JOIN role_menu rm ON m.menu_id = rm.menu_id
                 JOIN role r ON rm.role_id = r.role_id
                 JOIN user_role ur ON r.role_id = ur.role_id
                 JOIN user u ON ur.user_id = u.user_id
        WHERE u.user_id = #{userId}
          AND m.status = 1
          AND r.status = 1
        ORDER BY m.parent_id, m.order_num
    </select>
    <select id="selectMenuKeysByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM menu m
                 JOIN role_menu rm ON m.menu_id = rm.menu_id
                 JOIN role r ON rm.role_id = r.role_id
                 JOIN user_role ur ON r.role_id = ur.role_id
                 JOIN user u ON ur.user_id = u.user_id
        WHERE u.user_id = #{userId}
          AND m.status = 1
          AND m.perms IS NOT NULL
          AND m.perms != ''
    AND r.status = 1
        ORDER BY m.perms
    </select>
    <resultMap id="MenuResultMap" type="com.example.communityserver.entity.model.Menu">
        <id property="menuId" column="menu_id"/>
        <result property="menuName" column="menu_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="orderNum" column="order_num"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="menuType" column="menu_type"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="perms" column="perms"/>
        <result property="icon" column="icon"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <result property="isExternal" column="is_external"/>
        <result property="target" column="target"/>
        <result property="isDel" column="is_del"/>
    </resultMap>

    <select id="selectRoleMenus" resultMap="MenuResultMap">
        SELECT
            m.menu_id,
            m.menu_name,
            m.parent_id,
            m.order_num,
            m.path,
            m.component,
            m.menu_type,
            m.visible,
            m.status,
            m.perms,
            m.icon,
            m.create_time,
            m.update_time,
            m.remark,
            m.is_external,
            m.target,
            m.is_del
        FROM
            menu m
                INNER JOIN
            role_menu rm ON m.menu_id = rm.menu_id
        WHERE
            rm.role_id = #{roleId}
          AND m.is_del = 0
          AND m.status = 1
        ORDER BY
            m.parent_id, m.order_num
    </select>
</mapper>
